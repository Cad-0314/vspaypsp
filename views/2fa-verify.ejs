<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= t('title_2fa_verify') %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="container">
        <div class="lang-switch">
            <a href="?lang=en" class="lang-link <%= currentLang === 'en' ? 'active' : '' %>">English</a>
            <a href="?lang=zh" class="lang-link <%= currentLang === 'zh' ? 'active' : '' %>">中文</a>
        </div>

        <div class="logo-container">
            <img src="/images/logo.png" alt="VSPAY Logo" class="logo">
        </div>

        <h1>
            <%= t('header_verify') %>
        </h1>
        <p class="subtitle">
            <%= t('subtitle_enter_code') %>
        </p>

        <% if (typeof error !=='undefined' && error) { %>
            <div class="alert alert-error">
                <%= error %>
            </div>
            <% } %>

                <% if (typeof serverTime !=='undefined' ) { %>
                    <p style="font-size: 0.75rem; color: #6b7280; margin-bottom: 1rem;">
                        Server Time: <%= serverTime %>
                    </p>
                    <% } %>

                        <form action="/auth/2fa-verify" method="POST">
                            <div class="form-group">
                                <label for="token">
                                    <%= t('label_code') %>
                                </label>
                                <input type="text" id="token" name="token" required placeholder="<%= t('ph_code') %>"
                                    autocomplete="off" maxlength="6" pattern="[0-9]{6}" inputmode="numeric"
                                    style="text-align: center; font-size: 1.5rem; letter-spacing: 0.5em;">
                            </div>
                            <button type="submit" class="btn">
                                <%= t('btn_verify') %>
                            </button>
                        </form>
    </div>
</body>

</html>