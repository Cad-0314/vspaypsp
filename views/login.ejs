<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= t('title_login') %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="container">
        <div class="lang-switch">
            <a href="?lang=en" class="lang-link <%= currentLang === 'en' ? 'active' : '' %>">English</a>
            <a href="?lang=zh" class="lang-link <%= currentLang === 'zh' ? 'active' : '' %>">中文</a>
        </div>

        <div class="logo-container">
            <img src="/images/logo.png" alt="VSPAY Logo" class="logo">
        </div>

        <h1>
            <%= t('header_welcome') %>
        </h1>
        <p class="subtitle">
            <%= t('subtitle_login') %>
        </p>

        <% if (message && message.length> 0) { %>
            <div class="alert alert-error">
                <%= message %>
            </div>
            <% } %>

                <form action="/auth/login" method="POST">
                    <div class="form-group">
                        <label for="username">
                            <%= t('label_username') %>
                        </label>
                        <input type="text" id="username" name="username" required placeholder="<%= t('ph_username') %>">
                    </div>
                    <div class="form-group">
                        <label for="password">
                            <%= t('label_password') %>
                        </label>
                        <input type="password" id="password" name="password" required
                            placeholder="<%= t('ph_password') %>">
                    </div>
                    <button type="submit" class="btn">
                        <%= t('btn_login') %>
                    </button>
                </form>
    </div>
</body>

</html>