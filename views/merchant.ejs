<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= t('title_merchant') %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="container" style="max-width: 600px;">
        <div class="lang-switch">
            <a href="?lang=en" class="lang-link <%= currentLang === 'en' ? 'active' : '' %>">English</a>
            <a href="?lang=zh" class="lang-link <%= currentLang === 'zh' ? 'active' : '' %>">中文</a>
        </div>

        <div class="logo-container">
            <img src="/images/logo.png" alt="VSPAY Logo" class="logo">
        </div>

        <h1>
            <%= t('header_merchant') %>
        </h1>
        <p class="subtitle">
            <%= t('welcome_user', { username: user.username, role: user.role }) %>
        </p>

        <div style="text-align: left; background: #f9fafb; padding: 1rem; border-radius: 0.5rem; margin-bottom: 2rem;">
            <h3>
                <%= t('your_rates') %>
            </h3>
            <pre
                style="background: white; padding: 0.5rem; border: 1px solid #e5e7eb; border-radius: 0.25rem;"><%= user.channel_rates %></pre>
        </div>

        <a href="/auth/logout" class="btn" style="background-color: #ef4444;">
            <%= t('btn_logout') %>
        </a>
    </div>
</body>

</html>