<!DOCTYPE html>
<html lang="<%= currentLang %>">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Documentation - Payable</title>
    <!-- Fonts & Icons -->
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Fira+Code:wght@400;500&display=swap"
        rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/prismjs@1.29.0/prism.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/prismjs@1.29.0/themes/prism-tomorrow.min.css" rel="stylesheet">

    <style>
        :root {
            --primary: #6366f1;
            --primary-light: #e0e7ff;
            --sidebar-bg: #f8fafc;
            --border: #e2e8f0;
            --text-main: #0f172a;
            --text-muted: #64748b;
            --code-bg: #1e293b;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background: white;
            color: var(--text-main);
            display: flex;
            height: 100vh;
            overflow: hidden;
        }

        /* Sidebar */
        .sidebar {
            width: 280px;
            background: var(--sidebar-bg);
            border-right: 1px solid var(--border);
            padding: 1.5rem;
            height: 100%;
            overflow-y: auto;
            flex-shrink: 0;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-weight: 700;
            font-size: 1.25rem;
            margin-bottom: 2rem;
            color: var(--primary);
            text-decoration: none;
        }

        .nav-section {
            margin-bottom: 2rem;
        }

        .nav-title {
            font-size: 0.65rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            color: var(--text-muted);
            font-weight: 700;
            margin-bottom: 0.75rem;
        }

        .nav-link {
            display: block;
            padding: 0.5rem 0.75rem;
            color: var(--text-main);
            text-decoration: none;
            font-size: 0.875rem;
            border-radius: 0.375rem;
            margin-bottom: 0.125rem;
            font-weight: 500;
            transition: all 0.2s;
        }

        .nav-link:hover {
            background: var(--primary-light);
            color: var(--primary);
        }

        .lang-switch {
            display: flex;
            gap: 0.5rem;
            margin-bottom: 2rem;
        }

        .lang-btn {
            padding: 0.25rem 0.5rem;
            border-radius: 0.25rem;
            background: white;
            border: 1px solid var(--border);
            font-size: 0.75rem;
            cursor: pointer;
            text-decoration: none;
            color: var(--text-main);
        }

        .lang-btn.active {
            background: var(--primary);
            color: white;
            border-color: var(--primary);
        }

        /* Main Content */
        .content {
            flex: 1;
            padding: 3rem 6rem;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        section {
            margin-bottom: 4rem;
            max-width: 800px;
        }

        h1 {
            font-size: 2.25rem;
            margin-bottom: 1.5rem;
        }

        h2 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            margin-top: 2rem;
            border-bottom: 1px solid var(--border);
            padding-bottom: 0.5rem;
        }

        h3 {
            font-size: 1.125rem;
            margin-bottom: 0.75rem;
            margin-top: 1.5rem;
            color: var(--primary);
        }

        p {
            line-height: 1.6;
            color: var(--text-muted);
            margin-bottom: 1rem;
        }

        .endpoint {
            background: #f1f5f9;
            padding: 0.5rem 1rem;
            border-radius: 0.5rem;
            font-family: 'Fira Code', monospace;
            font-weight: 600;
            display: inline-block;
            margin-bottom: 1rem;
            font-size: 0.9rem;
        }

        .method {
            color: #059669;
            margin-right: 0.5rem;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 1.5rem;
            font-size: 0.875rem;
        }

        th,
        td {
            padding: 0.75rem;
            text-align: left;
            border-bottom: 1px solid var(--border);
        }

        th {
            background: #f8fafc;
            color: var(--text-muted);
            font-weight: 600;
        }

        code:not([class*="language-"]) {
            background: #f1f5f9;
            padding: 0.2rem 0.4rem;
            border-radius: 0.25rem;
            font-family: 'Fira Code', monospace;
            font-size: 0.85em;
            color: #e11d48;
        }

        .alert {
            padding: 1rem;
            border-radius: 0.5rem;
            background: #fef2f2;
            border-left: 4px solid #ef4444;
            color: #991b1b;
            margin-bottom: 1.5rem;
            font-size: 0.875rem;
        }

        pre[class*="language-"] {
            border-radius: 0.75rem;
            margin-bottom: 1.5rem;
            font-size: 0.85rem;
        }

        @media (max-width: 1024px) {
            .sidebar {
                transform: translateX(-100%);
                position: fixed;
                z-index: 100;
                transition: 0.3s;
            }

            .sidebar.show {
                transform: translateX(0);
            }

            .content {
                padding: 2rem;
            }
        }
    </style>
</head>

<body>

    <aside class="sidebar">
        <a href="/" class="logo">
            <i class="ri-rocket-2-fill"></i>
            Payable
        </a>

        <div class="lang-switch">
            <a href="?lang=en" class="lang-btn <%= currentLang === 'en' ? 'active' : '' %>">English</a>
            <a href="?lang=zh" class="lang-btn <%= currentLang === 'zh' ? 'active' : '' %>">中文</a>
        </div>

        <div class="nav-section">
            <div class="nav-title">GUIDE</div>
            <a href="#intro" class="nav-link">Introduction</a>
            <a href="#auth" class="nav-link">Authentication</a>
            <a href="#errors" class="nav-link">Error Codes</a>
        </div>

        <div class="nav-section">
            <div class="nav-title">PAY-IN</div>
            <a href="#payin-create" class="nav-link">Create Order</a>
            <a href="#payin-query" class="nav-link">Query Status</a>
            <a href="#payin-callback" class="nav-link">Callback</a>
        </div>

        <div class="nav-section">
            <div class="nav-title">PAY-OUT</div>
            <a href="#payout-bank" class="nav-link">Bank Transfer</a>
            <a href="#payout-usdt" class="nav-link">USDT Transfer</a>
            <a href="#payout-query" class="nav-link">Query Status</a>
        </div>

        <div class="nav-section">
            <div class="nav-title">UTILITIES</div>
            <a href="#balance" class="nav-link">Check Balance</a>
        </div>
    </aside>

    <main class="content">
        <% if (currentLang==='zh' ) { %>
            <!-- CHINESE CONTENT -->
            <section id="intro">
                <h1>API 文档</h1>
                <p>欢迎使用 Payable 支付网关 API。本文档提供了集成我们安全支付解决方案的全面指南。</p>
                <div class="alert">
                    <strong>版本:</strong> 2.1 | <strong>基础 URL:</strong> https://payable.firestars.co
                </div>
            </section>

            <section id="auth">
                <h2>身份验证</h2>
                <p>我们严格使用基于 Header 的身份验证。每个请求都必须包含以下头部信息。</p>
                <table>
                    <thead>
                        <tr>
                            <th>Header</th>
                            <th>说明</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td><code>x-merchant-id</code></td>
                            <td>您的唯一商户 UUID。</td>
                        </tr>
                        <tr>
                            <td><code>x-signature</code></td>
                            <td>请求正文的 HMAC-MD5 签名。</td>
                        </tr>
                    </tbody>
                </table>
                <h3>签名生成步骤</h3>
                <p>1. 将所有正文参数按键（Key）的 ASCII 码升序排序。<br>
                    2. 移除空值和 <code>sign</code> 字段（如果存在）。<br>
                    3. 使用 <code>&</code> 拼接为 <code>key=value</code> 格式。<br>
                    4. 在末尾追加 <code>&secret=您的商户密钥</code>。<br>
                    5. 进行 MD5 哈希并转换为大写。</p>
            </section>

            <section id="payin-create">
                <h2>创建收款订单</h2>
                <div class="endpoint"><span class="method">POST</span> /api/payin/create</div>
                <table>
                    <thead>
                        <tr>
                            <th>参数</th>
                            <th>类型</th>
                            <th>说明</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>orderId</td>
                            <td>String</td>
                            <td>商户唯一订单号</td>
                        </tr>
                        <tr>
                            <td>orderAmount</td>
                            <td>Number</td>
                            <td>收款金额 (最小 ₹100)</td>
                        </tr>
                        <tr>
                            <td>callbackUrl</td>
                            <td>String</td>
                            <td>回调通知地址</td>
                        </tr>
                        <tr>
                            <td>skipUrl</td>
                            <td>String</td>
                            <td>支付成功跳转地址</td>
                        </tr>
                    </tbody>
                </table>
            </section>

            <section id="payout-bank">
                <h2>银行代付</h2>
                <div class="endpoint"><span class="method">POST</span> /api/payout/bank</div>
                <table>
                    <thead>
                        <tr>
                            <th>参数</th>
                            <th>类型</th>
                            <th>说明</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>orderId</td>
                            <td>String</td>
                            <td>唯一交易号</td>
                        </tr>
                        <tr>
                            <td>amount</td>
                            <td>Number</td>
                            <td>代付金额 (最小 ₹100)</td>
                        </tr>
                        <tr>
                            <td>account</td>
                            <td>String</td>
                            <td>银行账号</td>
                        </tr>
                        <tr>
                            <td>ifsc</td>
                            <td>String</td>
                            <td>IFSC 代码</td>
                        </tr>
                        <tr>
                            <td>personName</td>
                            <td>String</td>
                            <td>收款人姓名</td>
                        </tr>
                    </tbody>
                </table>
            </section>

            <% } else { %>
                <!-- ENGLISH CONTENT -->
                <section id="intro">
                    <h1>API Documentation</h1>
                    <p>Welcome to the Payable Payment Gateway API. This documentation provides a comprehensive guide to
                        integrating our secure payment solutions.</p>
                    <div class="alert">
                        <strong>Version:</strong> 2.1 | <strong>Base URL:</strong> https://payable.firestars.co
                    </div>
                </section>

                <section id="auth">
                    <h2>Authentication</h2>
                    <p>We strictly use Header-Based Authentication. Every request must be authenticated using the
                        following headers.</p>
                    <table>
                        <thead>
                            <tr>
                                <th>Header</th>
                                <th>Description</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td><code>x-merchant-id</code></td>
                                <td>Your unique Merchant UUID.</td>
                            </tr>
                            <tr>
                                <td><code>x-signature</code></td>
                                <td>The HMAC-MD5 signature of your request body.</td>
                            </tr>
                        </tbody>
                    </table>
                    <h3>Signature Generation</h3>
                    <pre><code class="language-javascript">const crypto = require('crypto');

function generateSignature(params, secretKey) {
    const filtered = {};
    Object.keys(params).forEach(key => {
        if (key !== 'sign' && params[key] !== '' && params[key] != null) {
            filtered[key] = params[key];
        }
    });
    const sorted = Object.keys(filtered).sort();
    const query = sorted.map(k => `${k}=${filtered[k]}`).join('&');
    const str = `${query}&secret=${secretKey}`;
    return crypto.createHash('md5').update(str).digest('hex').toUpperCase();
}</code></pre>
                </section>

                <section id="errors">
                    <h2>Error Codes</h2>
                    <table>
                        <thead>
                            <tr>
                                <th>Code</th>
                                <th>Meaning</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>1</td>
                                <td>Success</td>
                            </tr>
                            <tr>
                                <td>0</td>
                                <td>Failed</td>
                            </tr>
                            <tr>
                                <td>-1</td>
                                <td>Invalid signature</td>
                            </tr>
                            <tr>
                                <td>-2</td>
                                <td>Missing parameters</td>
                            </tr>
                            <tr>
                                <td>-3</td>
                                <td>Insufficient balance</td>
                            </tr>
                        </tbody>
                    </table>
                </section>

                <section id="payin-create">
                    <h2>Create Order (Pay-In)</h2>
                    <p>Generate a payment link to collect funds from a user.</p>
                    <div class="endpoint"><span class="method">POST</span> /api/payin/create</div>

                    <h3>Request Body</h3>
                    <pre><code class="language-json">{
    "orderId": "MC001-998877",
    "orderAmount": 500,
    "callbackUrl": "https://mysite.com/webhook",
    "skipUrl": "https://mysite.com/success"
}</code></pre>
                </section>

                <section id="payout-bank">
                    <h2>Bank Transfer (Pay-Out)</h2>
                    <p>Withdraw funds to a bank account via IMPS/NEFT.</p>
                    <div class="endpoint"><span class="method">POST</span> /api/payout/bank</div>

                    <h3>Request Body</h3>
                    <pre><code class="language-json">{
    "orderId": "TXN_12345",
    "amount": 1000,
    "account": "1234567890",
    "ifsc": "SBIN0001234",
    "personName": "John Doe",
    "callbackUrl": "https://yoursite.com/payout/callback"
}</code></pre>
                </section>

                <section id="#payout-usdt">
                    <h2>USDT Transfer</h2>
                    <p>Withdraw funds to a USDT (TRC20) wallet.</p>
                    <div class="endpoint"><span class="method">POST</span> /api/payout/usdt</div>
                    <table>
                        <thead>
                            <tr>
                                <th>Parameter</th>
                                <th>Type</th>
                                <th>Description</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>orderId</td>
                                <td>String</td>
                                <td>Unique transaction ID</td>
                            </tr>
                            <tr>
                                <td>amount</td>
                                <td>Number</td>
                                <td>Amount in INR</td>
                            </tr>
                            <tr>
                                <td>walletAddress</td>
                                <td>String</td>
                                <td>TRC20 Wallet Address</td>
                            </tr>
                        </tbody>
                    </table>
                </section>

                <section id="balance">
                    <h2>Check Balance</h2>
                    <div class="endpoint"><span class="method">POST</span> /api/balance/query</div>
                    <p>Get your current available and pending balance.</p>
                </section>
                <% } %>
    </main>

    <script>
        // Smooth scrolling for navigation links
        document.querySelectorAll('.nav-link').forEach(link => {
            link.addEventListener('click', (e) => {
                const targetId = link.getAttribute('href');
                if (targetId.startsWith('#')) {
                    // Internal link behavior
                }
            });
        });
    </script>
</body>

</html>